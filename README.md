# cert-manager

0- Install the required packages
1- Open each Nginx config file, check if it's working with port 443, then update the certificate.
2- Add to crontab - every 3 months
3- update the nginx config file

```
sudo bash cert-manager.sh
```
